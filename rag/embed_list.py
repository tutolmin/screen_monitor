import os
from chromadb.config import Settings
from langchain_gigachat.embeddings.gigachat import GigaChatEmbeddings
from langchain_chroma import Chroma

# Убедитесь, что переменная окружения установлена
if "GIGACHAT_CREDENTIALS" not in os.environ:
    print("❌ Ошибка: GIGACHAT_CREDENTIALS не установлена")
    exit(1)

# Используем те же эмбеддинги, что и при создании базы
embeddings = GigaChatEmbeddings(
    credentials=os.environ["GIGACHAT_CREDENTIALS"],
    verify_ssl_certs=False
)

#persist_directory = "./chroma_db_p_web"
#persist_directory = "./chroma_db_p_pres"
#persist_directory = "/home/andrei/chroma_db_u_pres"
#persist_directory = "/home/andrei/chroma_db_u_pres_cos"
#persist_directory = "/home/andrei/chroma_db_c_pres"
persist_directory = "/home/andrei/chroma_db_c_pres_cos"
#persist_directory = "./chroma_db_u_web"
#persist_directory = "./chroma_db_f_web"
#persist_directory = "./chroma_db_f_pres"
#persist_directory = "./chroma_db_e_web"
#persist_directory = "./chroma_db_e_pres"

# Загружаем базу
db = Chroma(
    persist_directory=persist_directory,
    embedding_function=embeddings
)

# ЛИСТИНГ ВСЕХ ЧАНКОВ
print("=" * 80)
print("📋 ЛИСТИНГ ВСЕХ ЧАНКОВ В БАЗЕ:")
print("=" * 80)

try:
    # Получаем все чанки из базы
    all_data = db.get()

    if all_data and 'documents' in all_data and len(all_data['documents']) > 0:
        total_chunks = len(all_data['documents'])
        print(f"✅ Всего чанков в базе: {total_chunks}")
        print("-" * 80)
        
        for i, (doc_content, metadata, doc_id) in enumerate(zip(
            all_data['documents'],
            all_data['metadatas'],
            all_data['ids']
        )):
            continue
            print(f"🎯 ЧАНК #{i+1}")
            print(f"   ID: {doc_id}")
            print(f"   Содержимое: {doc_content}")
            print(f"   Метаданные: {metadata}")
            print(f"   Длина: {len(doc_content)} символов")
            print("-" * 80)

    else:
        print("❌ В базе нет чанков или база пуста")

except Exception as e:
    print(f"❌ Ошибка при получении чанков: {e}")

# Тестируем поиск
print("\n🔍 ТЕСТИРУЕМ ПОИСК:")
try:
    if all_data and 'documents' in all_data and len(all_data['documents']) > 0:
        # Берем часть из первого чанка для тестового запроса
#        first_chunk_content = all_data['documents'][0]
#        test_query = first_chunk_content[:50] + "..."
#        test_query = "наивный"
#        test_query = "любознательность"
#        test_query = "К конструктивной агрессии можно отнести (множественный выбор):"
        test_query = """
СБЕР
УНИВЕРСИТЕТ
Сбер Мини-МВА 16 поток Экзамен по дисциплине "Комму
К конструктивной агрессии можно отнести (множественный выбор):
Напористость
Неуступчивость
Любознательность
Нетерпимость
"""

        test_query = """
Переговорная карта может быть (множественный выбор):
Тактическая
B
Пестрая
Наивная
Комплексная
"""

        test_query = """
Что относится к источникам переговорной силы (мночественный выбор):
Сила коалиции
B
Сила стратегии
Сила ситуации
D
Сила личности
Сила здравого смысла
Сила давления
"""

        test_query = """
Подготовка к гибкому диалогу состоит из (множественный выбор):
3-х Уровневого анализа позиций
B Приоритизации
Моделирования обменов
Декомпозиции
Симуляции
"""
        test_query = """
Для чего используется диаграмма Ишикавы?
"""

        test_query = """
Укажите свойства системы 1. Выберите три варианта.
"""

        test_query = """
иррациональная тоска
"""
        test_query = """
Из перечисленных ниже выберите 3 принципа поиска нестандартных решений:
"""

        test_query = """
Межвременное дисконтирвание
"""
        test_query = """
На какой схеме SWOT-анализа корректно обозначены квадранты (области)
"""
        test_query = """
Главная задача, решаемая нами при заполнении колонки «контекст» в карточке проблемы это...
"""

        test_query = """
Из перечисленных ниже характеристик обработки информации выберите те, которые относятся к познавательной Системе 2
"""

        test_query = "трендспоттер"
        test_query = "ras"
        test_query = "негативные установки"
        test_query = "сервисный процесс"
        test_query = "инсайт"
        test_query = "триггер"
        test_query = "апгар"
        test_query = "себестоимость"
        test_query = "Что является важными составляющими клиентского опыта (множественный выбор):\n\nПолезность\nУдобство\nПрямолинейность\nПриятность\nСкорость\nЗапоминаемость"
#        test_query = "Удобство\nПрямолинейность\nПриятность\nСкорость\nЗапоминаемость\nПолезность\n"
        test_query = "CJM"
        test_query = "NPS"
        test_query = "EBITDA"
        test_query = "TRI"
#        test_query = "развивается эмоциональный интеллект"
        test_query = "Вы проводили исследование сервисного сценария для внешнего клиента. В процессе исследований вы выяснили, что “путь клиента” имеет множество провалов впечатлений. Причиной данных провалов часто являются действия сотрудников Банка, в частности, клиентских менеджеров, которые непосредственно взаимодействуют с клиентами. Изменения, которые вы предлагаете внедрить будут касаться оптимизации процессов и внедрению нового сценария поведения клиентского менеджера при взаимодействии с клиентом. В данной ситуации клиентский менеджер в рамках проекта:"
        test_query = "Стейкхолдер"
        test_query = "Владелец процесса"
        test_query = "Клиент проекта"

        test_query = """
Для команды с выраженной культурой «Приказ и контроль» характерно:
Высокая техническая экспертиза и нацеленность на поиск уникальных решений
"""
#Разделение на принимающих решение и исполнителей
#        test_query = "Разделение на принимающих решение и исполнителей"
#        test_query = "Плоская структура с высоким уровнем креатива"
#        test_query = "Высокая техническая экспертиза и нацеленность на поиск уникальных решений"
#        test_query = "Все вышеперечисленное"

        test_query = """
Одно из самых известных олицетворений теории игр в жизни это:
"""
#Теорема узника
#Проблема каторжников
#Жизнь бродяги
#Дилемма заключенного
#Дихотомия арестанта

        test_query = """
Переговорная карта
"""
        q = """
Повестка переговоров состоит из обсуждения:
Цели, условий потенциальной сделки и продолжительности переговоров;
Первых предложений и всех разногласий сторон;
Ценности условий, норм взаимодействия в переговорах и возможностей завершения переговоров;
Переговорная карта может быть (множественный выбор):

Пестрая
Наивная
Комплексная
Тактическая
"""

        print(f"Тестовый запрос: '{test_query}'")
#        results = db.similarity_search(test_query, k=min(5, len(all_data['documents'])))
        results = db.similarity_search_with_score(test_query, k=min(3, len(all_data['documents'])))

#        results = db.max_marginal_relevance_search(
#            test_query,
#            k=5,
#            fetch_k=15,
#            lambda_mult=0.7
#        )

        print(f"✅ Найдено результатов: {len(results)}")

#        for i, doc, score in enumerate(results):
        for i, (doc, score) in enumerate(results):
            print(f"\n   Найденный чанк #{i+1}:")
            print(f"   Содержимое: {doc.page_content}")
            print(f"   Метаданные: {doc.metadata}")
            print(f"   Сходство (расстояние): {score:.4f}")
    else:
        print("❌ Нет данных для тестирования поиска")

except Exception as e:
    print(f"❌ Ошибка при тестировании поиска: {e}")
